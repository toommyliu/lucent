{
  "name": "@lucent/docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "blume dev",
    "build": "blume build --strict && blume validate --strict",
    "preview": "blume preview",
    "check": "blume check --strict",
    "validate": "blume validate --strict"
  },
  "dependencies": {
    "blume": "^1.2.1",
    "minisearch": "^7.2.0"
  },
  "devDependencies": {
    "astro": "^7.0.2"
  }
}
